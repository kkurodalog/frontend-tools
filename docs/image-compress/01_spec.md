# 01_spec — 画像仕分け圧縮くん 詳細仕様

> ⚠️ F1 雛形（見出し骨格のみ）。中身の本格執筆は **F2 設計フェーズ**で埋める（担当: Mado=要件・UX / Mane=スコープ・工数管理）。
> 元素材: `mado-selfuse-dev-tools.md`（§E-1 6種別プリセット表）＋ `mado-image-tool-spec-proposal.md`（DECISION LIST D1〜D10）。両者は tmp に原本残置（移管表対象だが本ファイルの「元素材」として参照）。
> v1スコープは確定済み（U-01 / 案②フル）。`../../PLAN.md` §2-1 参照。

---

## 1. 種別プリセット（6種別）⇄ パラメータ確定表

<!-- F2: ①背景・装飾＝強圧縮 ②写真（重要）＝画質優先 ③写真（一般）＝中圧縮 ④イラスト・図版 ⑤アイコン・ロゴ（SVG推奨警告） ⑥スクショ・UI（可逆優先）
     各プリセットの「目標フォーマット／品質／リサイズ上限／設計意図」を表で確定する -->

## 2. DECISION LIST（D1〜D10）の実装反映

<!-- F2: PLAN.md §2-1 の D1〜D10 確定結果を仕様に落とす（HEIC非対応／自動種別推測＋トグル／EXIF削除デフォルトON／AVIF含む／⑥可逆PNG／劣化確認UI／JPEG穏やか最適化／枚数サイズ上限／フォルダ一括任意／出力ファイル名） -->

## 3. WASM エンコーダ選定

<!-- F2: @jsquash/webp・@jsquash/avif・@jsquash/oxipng・imagequant 等。Web Worker 逐次処理（AVIF重い対策）を含む -->

## 4. UI 設計

<!-- F2: D&D → 全体一括の種別選択 ＋ 各画像で個別に種別変更・手動オーバーライド可（quality/フォーマット/リサイズ）。Before/After・劣化確認ルーペ・フォーマット別容量比較・一括ZIP DL -->

## 5. 自動種別推測ロジック

<!-- F2: 解像度・透過・色数からデフォルト割当を推測。ON/OFFトグル・初期値・手動修正可・根拠表示・精度は追わない・実装は最後に積む（D2） -->

## 6. 入力・出力仕様

<!-- F2: 入力枚数/サイズ上限（一度に20〜30枚・〜25MB目安・実機調整）／出力ファイル名（元名維持＋拡張子変換追従）／ZIP一括 -->

---

## 関連ドキュメント
- `00_overview.md` — 概要・痛点・スコープ
- `02_qa.md` — QAチェックリスト
- `../../PLAN.md` §2-1 / §3 — v1スコープ・工数の正本
