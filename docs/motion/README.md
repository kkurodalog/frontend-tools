# motion — スクロール演出ジェネレーター（温存ツール）

第1弾の後の有力候補。スクロール演出を3プリセット＋AIプロンプト出力で生成するツール。看板＝スクロール特化 / 内部設計＝カテゴリ追加で育つ汎用器 / URL＝`/tools/motion/`（旧 `motion.kurodafolio.com` サブドメイン案を吸収）。

> ⚠️ F1 では温存（素材移管のみ）。本格設計・実装は U-04（今後ツールの優先順）確定後の別フェーズ。
> 正本方針は `../../PLAN.md` §2-6 を参照。

---

## 移管素材（tmp/tool-ideation/ からコピー・原本残置）
| ファイル | 内容 | 旧サブドメイン読み替え注記 |
|---|---|---|
| `mado-scroll-gen-spec.md` | スクロール演出ツール仕様（v1=3プリセット＋AIプロンプト出力・完成済み） | 付与済み |
| `mado-scope-decision.md` | スコープ判断（看板＝特化 / 中身＝汎用器） | 付与済み |

---

## 今後（U-04 で着手順が決まったら）
- `00_overview / 01_spec / 02_qa / 03_seo` の軽量4点に整理（image-compress と同じ構成）
- slug = `motion`（URL・ソースフォルダ・docsフォルダで完全一致）
